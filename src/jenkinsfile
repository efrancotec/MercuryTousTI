pipeline{
    agent { label 'master'}

    stages{
        stage ('test'){
            steps{
                dir("build_java"){
                    sh "mvn clean compile test"
                }
            }
        }
        stage ('Build application'){
            steps{
                echo "Mvn clean install"
            }
        }
        stage ('create docker image'){
            steps{
                echo "Creando docker"
            }
        }
    }
}
